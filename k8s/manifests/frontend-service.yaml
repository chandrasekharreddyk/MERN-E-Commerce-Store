apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  namespace: mern-dev
  labels:
    app: mern-frontend
spec:
  type: NodePort
  selector:
    app: mern-frontend
  ports:
  - port: 80
    targetPort: 80
    nodePort: 30080